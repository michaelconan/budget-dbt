macros:
  - name: test_column_values_match_regex
    description: Test to check if a string matches a regular expression pattern using SQLite extension syntax.
    arguments:
      - name: model
        type: string
        description: "Model Name"
      - name: column_name
        type: string
        description: "Column name that should not be an empty string"
      - name: pattern
        type: str
        description: "Regular expression pattern to match with option flags"